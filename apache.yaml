- name: Install Apache
  host: webservers
  become: true

  tasks:
    - name: Install Apache
      apt:
        name: apache2
        state: lastest
        update_cache: yes
      when: ansible_os_family == "Debian" 